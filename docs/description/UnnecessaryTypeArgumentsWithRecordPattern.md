<div>

Checks for unnecessary explicit type arguments in record patterns.

</div>

When a record pattern declares explicit type arguments that can be
inferred by the compiler, those type arguments are redundant and reduce
readability.
